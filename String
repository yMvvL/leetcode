//String
#include<bits/stdc++.h>
#include<climits>
using namespace std;
#define ll long long
#define IOS ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)
#define itn int
#define vetcor vector
#define asn ans

int main(){
	IOS;
	int t;
	cin>>t;
	vector<string> s(t);
	vector<int> ans(t,0);
	for(int i=0;i<t;i++){
		cin>>s[i];
		for(int j=0;j<s[i].size();j++){
			if(s[i][j]=='1'){
				ans[i]++;
			}
		}
	}
	for(int i=0;i<t;i++){
		cout<<ans[i]<<endl;
	}
	return 0; 
} 
